
const User = require('./User');
const Investment = require('./Investment');
const ReferralIncome = require('./ReferralIncome');
const RoiHistory = require('./RoiHistory');

module.exports = {
  User,
  Investment,
  ReferralIncome,
  RoiHistory,
};
